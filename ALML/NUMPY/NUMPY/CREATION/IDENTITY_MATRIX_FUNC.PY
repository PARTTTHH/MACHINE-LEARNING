# IDENTITY MATRIX OR EYE FUNCTION  1s ON THE DIAGONAL AND ZEROs ELSE WHERE
# eye(size)   EYE FUNCTION

import numpy as np

identity_matrix = np.eye(5)
print(identity_matrix)
print(type(identity_matrix))